export type ProjectInterface = {
    tag: string;
    title: string;
    desc: string;
};

export type NavLinkType = {
    text: string;
    hrefLink: string;
};

export type StyledHeadingType = {
    title: string;
};
